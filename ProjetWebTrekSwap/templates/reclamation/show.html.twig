{% extends 'base.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_rec</th>
                <td>{{ reclamation.idRec }}</td>
            </tr>
            <tr>
                <th>Description_rec</th>
                <td>{{ reclamation.descriptionRec }}</td>
            </tr>
            <tr>
                <th>Date_rec</th>
                <td>{{ reclamation.dateRec ? reclamation.dateRec|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type_rec</th>
                <td>{{ reclamation.typeRec }}</td>
            </tr>
            <tr>
                <th>Etat_rec</th>
                <td>{{ reclamation.etatRec }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_reclamation_edit', {'id_rec': reclamation.idRec}) }}">edit</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
